<section id="descarga" class="descarga">
  <div class="descarga-card">
    <p class="download-badge">Descarga gratuita</p>
    
    <h2 class="title">
      EMPIEZA HOY.<br />
      <span>GRATIS, PARA SIEMPRE</span>
    </h2>

    <p class="desc">
      Sin costos ocultos. Sin tarjeta requerida. El plan gratuito de FinTrack cubre todo lo que necesitas para empezar a tomar el control de tus finanzas ahora mismo.
    </p>

    <a href="https://github.com/AngelVilla16/Fintrack-54/releases/download/Mobile/fintrack-3.1.apk" class="btn-download">
      ¡Descárgalo ahora!
    </a>
  </div>
</section>

<style>
  .descarga {
    max-width: 1000px;
    margin: 0 auto;
    padding: 100px 40px 120px;
    text-align: center;
    font-family: var(--font-body, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif);
  }

  @media (max-width: 768px) {
    .descarga {
      padding: 80px 24px 100px;
    }
  }

  @media (max-width: 640px) {
    .descarga {
      padding: 56px 16px 80px;
    }
  }

  .descarga-card {
    background: linear-gradient(180deg, var(--bg-card, #10161c) 0%, rgba(13, 42, 32, 0.3) 100%);
    border: 1px solid var(--border, rgba(255, 255, 255, 0.08));
    border-radius: var(--radius-lg, 22px);
    padding: 60px 30px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3), 0 0 20px rgba(0, 164, 101, 0.05);
  }

  .download-badge {
    display: inline-block;
    font-family: monospace;
    font-size: 0.8rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--green, #00a465);
    background: var(--green-soft, rgba(0, 164, 101, 0.12));
    border: 1px solid rgba(0, 164, 101, 0.4);
    border-radius: 100px;
    padding: 6px 16px;
    font-weight: 700;
    margin-bottom: 24px;
  }

  .descarga .title {
    font-size: clamp(2rem, 4vw, 2.8rem);
    font-weight: 800;
    line-height: 1.2;
    color: var(--text, #f5f7f8);
    letter-spacing: -0.01em;
  }

  .descarga .title span {
    color: var(--green, #00a465);
  }

  .descarga .desc {
    margin: 22px auto 0;
    color: var(--text-muted, #9aa5ac);
    font-size: 1.05rem;
    max-width: 560px;
    line-height: 1.6;
  }

  .btn-download {
    display: inline-block;
    margin-top: 36px;
    background: var(--green, #00a465);
    color: #ffffff;
    padding: 16px 36px;
    border-radius: 100px;
    font-weight: 700;
    font-size: 1rem;
    text-decoration: none;
    transition: transform 0.2s ease, background 0.2s ease, box-shadow 0.2s ease;
    box-shadow: 0 4px 16px rgba(0, 164, 101, 0.3);
  }

  .btn-download:hover {
    background: #00c078;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0, 164, 101, 0.45);
  }

  @media (max-width: 480px) {
    .descarga-card {
      padding: 40px 20px;
    }
  }
</style>