<section class="video-section">
  <div class="video-head">
    <p class="title">DEMOSTRACIÓN</p>
    <h2 class="subtitle">Conoce FinTrack en acción</h2>
  </div>

  <div class="video-wrapper">
    <video controls preload="metadata" poster="/assets/video-poster.png">
      <source src="/Fintrack-720.mp4" type="video/mp4" />
      Tu navegador no soporta la reproducción de video.
    </video>
  </div>
</section>

<style>
  .video-section {
    padding: 90px 40px;
    max-width: 1000px;
    margin: 0 auto;
    text-align: center;
    font-family: var(--font-body, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif);
  }

  @media (max-width: 768px) {
    .video-section {
      padding: 72px 24px;
    }
  }

  @media (max-width: 640px) {
    .video-section {
      padding: 56px 16px;
    }
  }

  .video-head .title {
    font-family: monospace;
    font-size: 0.8rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--green, #00a465);
    font-weight: 700;
    margin-bottom: 16px;
  }

  .video-head .subtitle {
    font-size: clamp(1.9rem, 3.6vw, 2.6rem);
    font-weight: 800;
    line-height: 1.15;
    letter-spacing: -0.01em;
    color: var(--text, #f5f7f8);
    margin-bottom: 48px;
  }

  /* Contenedor responsivo con relación de aspecto 16:9 */
  .video-wrapper {
    position: relative;
    width: 100%;
    max-width: 860px;
    margin: 0 auto;
    border-radius: var(--radius-lg, 22px);
    border: 1px solid var(--border, rgba(255, 255, 255, 0.08));
    background: var(--bg-card, #10161c);
    overflow: hidden;
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.4), 0 0 30px rgba(0, 164, 101, 0.1);
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
  }

  .video-wrapper:hover {
    border-color: rgba(0, 164, 101, 0.4);
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5), 0 0 40px rgba(0, 164, 101, 0.18);
  }

  .video-wrapper video {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 16 / 9;
    object-fit: cover;
    outline: none;
  }
</style>