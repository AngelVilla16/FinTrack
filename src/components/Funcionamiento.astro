<section id="funcionamiento" class="funcionamiento">
  <div class="fun-head">
    <p class="title">CÓMO FUNCIONA</p>
    <h2 class="subtitle">En marcha en menos de 3 minutos.</h2>
  </div>

  <div class="grid-funcionamiento">
    <div class="funCard">
      <div class="step-badge">01</div>
      <h3>Crea tu cuenta</h3>
      <p>Regístrate en segundos con tu correo electrónico. ¡Sin tarjetas ni datos bancarios requeridos!</p>
    </div>

    <div class="funCard">
      <div class="step-badge">02</div>
      <h3>Comienza a registrar</h3>
      <p>Con nuestro menú intuitivo serás capaz de registrar rápidamente tus ingresos y gastos diarios.</p>
    </div>

    <div class="funCard">
      <div class="step-badge">03</div>
      <h3>Toma el control</h3>
      <p>Visualiza y analiza de manera rápida y ordenada los porcentajes de tus movimientos por categoría y tu saldo en tiempo real.</p>
    </div>
  </div>
</section>

<style>
  .funcionamiento {
    padding: 90px 40px;
    max-width: 1180px;
    margin: 0 auto;
    text-align: center;
    font-family: var(--font-body, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif);
  }

  @media (max-width: 768px) {
    .funcionamiento {
      padding: 72px 24px;
    }
  }

  @media (max-width: 640px) {
    .funcionamiento {
      padding: 56px 20px;
    }
  }

  .fun-head .title {
    font-family: monospace;
    font-size: 0.8rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--green, #00a465);
    font-weight: 700;
    margin-bottom: 16px;
  }

  .fun-head .subtitle {
    font-size: clamp(1.9rem, 3.6vw, 2.6rem);
    font-weight: 800;
    line-height: 1.15;
    letter-spacing: -0.01em;
    color: var(--text, #f5f7f8);
  }

  .grid-funcionamiento {
    margin-top: 56px;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    text-align: left;
  }

  @media (max-width: 992px) {
    .grid-funcionamiento {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .grid-funcionamiento {
      grid-template-columns: 1fr;
    }
  }

  .funCard {
    background: var(--bg-card, #10161c);
    border: 1px solid var(--border, rgba(255, 255, 255, 0.08));
    border-radius: var(--radius-md, 14px);
    padding: 30px 26px;
    transition: transform 0.25s ease, border-color 0.25s ease, box-shadow 0.25s ease;
    display: flex;
    flex-direction: column;
    position: relative;
  }

  .funCard:hover {
    transform: translateY(-4px);
    border-color: var(--green, #00a465);
    box-shadow: 0 8px 24px rgba(0, 164, 101, 0.12);
  }

  .step-badge {
    font-family: monospace;
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--green, #00a465);
    background: var(--green-soft, rgba(0, 164, 101, 0.12));
    border: 1px solid rgba(0, 164, 101, 0.3);
    width: 36px;
    height: 36px;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 20px;
  }

  .funCard h3 {
    font-size: 1.2rem;
    font-weight: 700;
    color: var(--text, #f5f7f8);
    margin-bottom: 12px;
    line-height: 1.3;
  }

  .funCard p {
    font-size: 0.95rem;
    color: var(--text-muted, #9aa5ac);
    line-height: 1.6;
  }

  @media (max-width: 480px) {
    .funCard {
      padding: 22px 20px;
    }
  }
</style>